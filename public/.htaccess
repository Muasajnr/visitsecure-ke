RewriteEngine On

# Block direct access to dotfiles
<FilesMatch "^\.">
    Require all denied
</FilesMatch>

# If the request is not for an existing file or directory, send it to index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
